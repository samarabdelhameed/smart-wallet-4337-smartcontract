// SPDX-License-Identifier: MIT
pragma solidity 0.8.21;

library Base64URL {
    function encode(bytes memory data) internal pure returns (string memory) {
        // Dummy implementation: just return an empty string for now
        // (مؤقت بس عشان الكمبايل يشتغل، نطوره بعدين لو احتجنا)
        return "";
    }
}
